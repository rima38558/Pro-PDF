repos:
  - repo: local
    hooks:
      - id: check-alembic-present
        name: Check Alembic migration present when models change
        entry: python scripts/check_alembic_present.py
        language: system
        stages: [commit]
        files: '.*'
