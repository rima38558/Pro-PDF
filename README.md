Pro PDF — dev README
=====================

[![Codecov](https://codecov.io/gh/rima38558/Pro-PDF/branch/ci/add-github-actions/graph/badge.svg)](https://codecov.io/gh/rima38558/Pro-PDF)
[![Coveralls](https://coveralls.io/repos/github/rima38558/Pro-PDF/badge.svg?branch=ci/add-github-actions)](https://coveralls.io/github/rima38558/Pro-PDF)

This repository contains a FastAPI backend with Celery workers for PDF processing and a docker-compose setup for local development.

Quick start (Linux/macOS/Windows with Docker):

1. Ensure Docker and Docker Compose are installed.
2. (Optional) Copy `.env.example` to `.env` and set secrets (`DATABASE_URL`, `JWT_SECRET`, `USE_S3`, etc.).
3. Run migrations once:

```bash
make migrate
```

4. Start the stack:

```bash
make up
```

Useful targets:

- `make migrate` — run Alembic migrations (one-off service)
- `make up` — build and start containers
- `make worker` — run a Celery worker
- `make logs` — follow logs
- `make shell` — open a shell in the backend image

Notes
- Run `make migrate` before `make up` to ensure DB schema is applied.
- For S3/MinIO, set `USE_S3=true` and provide `S3_ENDPOINT`, `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `S3_BUCKET` in `.env`.

Development
-----------

Recommended local development setup (Windows PowerShell example):

```powershell
cd "C:\xampp\htdocs\Pro PDF"
py -3.11 -m venv .venv
& ".\.venv\Scripts\Activate.ps1"
python -m pip install -U pip
python -m pip install -r backend/requirements.txt
python -m pip install -e backend
python -m pytest -q
```

Notes:
- Installing the backend in editable mode (`pip install -e backend`) lets tests import the `app` package without setting `PYTHONPATH`.
- If you prefer not to install editable, set `PYTHONPATH` to `backend` when running tests: `$env:PYTHONPATH = "C:\xampp\htdocs\Pro PDF\backend"`.

Generating Alembic migrations locally
------------------------------------

When you change SQLAlchemy models, generate a migration locally (don't run `--autogenerate` in CI).

1. Create a concise message describing the change and pass it to the Makefile target, for example:

```bash
make alembic-rev NAME="add_user_verified_field"
```

2. Review the generated migration under `backend/alembic/versions/` and adjust if necessary.

3. Commit the migration file and push the branch.

Notes:
- The `make alembic-rev` target installs dependencies and runs `alembic -c backend/alembic.ini revision --autogenerate -m "$NAME"`.
- Always inspect autogenerated migrations to ensure they match your intent.

Pre-commit hook
--------------

We include a lightweight pre-commit hook that prevents committing changes to `backend/app` model files unless a migration under `backend/alembic/versions/` is staged.

Install and enable pre-commit locally:

```bash
python -m pip install --user pre-commit
pre-commit install
```

The hook runs `scripts/check_alembic_present.py` and will block the commit with instructions if a migration is missing.

Email & SendGrid
-----------------

To enable email sending (verification/reset/invoices) configure these environment variables:

- `SENDGRID_API_KEY` — your SendGrid API key
- `EMAIL_FROM` — sender email address (default `noreply@pro-pdf.local`)
- `APP_URL` — frontend URL used to build verification/reset links (e.g. `https://app.example.com`)

The app queues emails via Celery; ensure `redis` and workers are running for background delivery.

Development fallback
--------------------

If `SENDGRID_API_KEY` is not set, the app will write outgoing emails to `./backend/data/emails` as JSON files. This makes local testing safe and visible without sending external emails.

To enable SendGrid for real delivery, configure `SENDGRID_API_KEY` and set `EMAIL_FROM`/`APP_URL` accordingly.

